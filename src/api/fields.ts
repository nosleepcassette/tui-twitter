export const DEFAULT_TWEET_FIELDS =
  "created_at,public_metrics,conversation_id,in_reply_to_user_id,referenced_tweets,author_id,attachments";

export const DEFAULT_USER_FIELDS =
  "name,username,description,profile_image_url,public_metrics,verified";

export const DEFAULT_MEDIA_FIELDS = "media_key,type,url,preview_image_url,width,height,alt_text";

export const DEFAULT_EXPANSIONS = "author_id,referenced_tweets.id,attachments.media_keys";
